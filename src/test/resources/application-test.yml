spring:
  datasource:
    url: jdbc:h2:mem:rules;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop
argus:
  rules:
    # Tests assert on what they insert; seeded examples would make counts meaningless.
    seed-examples: false
